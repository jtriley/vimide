vimide - VIM as an Integrated Development Environment

Installation:
-------------

$ git clone git://github.com/jtriley/vimide.git
$ mv vimide ~/.vim
$ ln -s ~/.vim/vimrc ~/.vimrc
$ vim /some/file/and/test/it/out
